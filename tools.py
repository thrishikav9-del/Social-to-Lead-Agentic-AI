def mock_lead_capture(name: str, email: str, platform: str):
    """
    Mock backend API function that captures the lead after all details are provided.
    """
    print(f"\n[BACKEND SYSTEM] Lead captured successfully: {name}, {email}, {platform}")
